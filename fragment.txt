#version 300 es

#ifdef GL_ES
precision highp float;
#endif // GL_ES

layout(location = 0) out vec4 rtFragColor;
//out vec4 rtFragColor; //Automatic way

// Per Vertex: final color inbound
//in vec4 vColor;

//Per Fragment receive requirements for final color
in vec4 vNormal;

void main()
{
	//rtFragColor = vec4(0.5, 1, 0, 1);
	//rtFragColor = vColor;
	
	// Per Fragment: caculate and display
	vec4 N = normalize(vNormal);
	rtFragColor = vec4(N.xyz * 0.5 + 0.5, 1.0);
}